README

This repository is the portfolio site. The chatbot and server proxy were removed per request. All UI is client-side static HTML/CSS/JS.

If you'd like me to re-add a secure server or a serverless function in the future, I can implement it behind an authenticated endpoint.

